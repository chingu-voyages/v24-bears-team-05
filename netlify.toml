[build]
  command = "npm run build"
  publish = "public/"
  functions = "functions"
[build.environment]
  NODE_VERSION = "14"

[[headers]]
  for = "/svelte/*"
  [headers.values]
    cache-control = '''
      public,
      max-age=31536000,
      immutable'''